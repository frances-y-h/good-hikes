extends layout


include utils.pug

block content

block content
    link(rel='stylesheet' href='/stylesheets/collection.css')
    script(src='/javascripts/editCollection.js')
    
    #edit-content-div
        h2.center Edit Collections
        form#form-add-collection(method="post" action="/collections/edit/new")
            input(type='hidden' name='_csrf' value=csrfToken)
            input#new-collection-input(name="collectionname" required placeholder="Add Collection name" value=collectionname)
            button.btn#add-collection-button(type="submit") Add Collection
        +validationErrorSummary(errors)
        table 
            colgroup
                col#delete-col
                col#name-col
                col#rename-col
            thead
                tr.hike-row
                    th Delete 
                    th.left Collection Name
                    th
                tbody 
                    each collection in collections 
                        tr.hike-row 
                            td.center                            
                                form(method='post' action=`/collections/${collection.id}/delete`)
                                    input(type='hidden' name='_csrf' value=csrfToken)
                                    button.btn-delete
                                        i.fa.fa-times(id=`${collection.id}-delete-button`)
                            td= collection.name 
                            td.center
                                form(method='post' action=`/collections/${collection.id}/edit`)
                                    input(type='hidden' name='_csrf' value=csrfToken)
                                    button.btn.btn-rename(id=`${collection.id}-rename-button`) Rename
        //- .bg-modal.hidden
        //-     form(id='rename-form' class="hidden form-control")
        //-         h2#rename-title
        //-         input(type='hidden' name='_csrf' value=csrfToken)
        //-         input#rename-collection-input(name="collectionname" required placeholder=`Enter new collection name` value=collectionname)
        //-         #rename-button-div
        //-             button.btn#rename-collection-button Rename Collection
        //-             button.btn.btn-cancel#cancel-collection-button Cancel





    
